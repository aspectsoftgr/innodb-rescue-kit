# Security

This repository contains recovery scripts that handle database credentials and dumps.

Do not publish:

- SQL dumps,
- passwords,
- real grants files,
- private database names,
- application secrets.

Before publishing logs or issue reports, redact table names and values if they reveal sensitive information.
